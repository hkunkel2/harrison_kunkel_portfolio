<div class="container-fluid w-50 p-5">
  <h1 class="text-center">Edit <%= resource_name.to_s.humanize %></h1>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="mb-3">
      <%= f.label :email, :class => "form-label"%><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => "form-control" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="mb-3">
      <%= f.label :password, :class => "form-label" %> <i>(leave blank if you don't want to change it)</i><br />
      <%= f.password_field :password, autocomplete: "new-password", :class => "form-control" %>
      <% if @minimum_password_length %>
        <br />
        <em><%= @minimum_password_length %> characters minimum</em>
      <% end %>
    </div>

    <div class="mb-3">
      <%= f.label :password_confirmation, :class => "form-label" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", :class => "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.label :current_password, :class => "form-label" %> <i>(we need your current password to confirm your changes)</i><br />
      <%= f.password_field :current_password, autocomplete: "current-password", :class => "form-control" %>
    </div>

    <div class="text-center d-grid gap-2 col-6 mx-auto mb-2">
      <%= f.submit "Update", :class => "btn btn-success" %>
    </div>

  <% end %>

  <div class="text-center d-grid gap-2 col-6 mx-auto mb-2">
    <div>
      <h3>Unhappy?</h3>
      <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, :class => "btn btn-danger" %>
    </div>
  </div>
  <div class="text-center d-grid gap-2 col-1 mx-auto mb-2">
    <%= link_to "Back", :back, :class => "btn btn-outline-primary" %>
  </div>
</div>





<div>
    <h4>Example Output:</h4>
  {% if product.metafields.custom.example_output != null and product.metafields.custom.example_output != "" %}
    {{ product.metafields.custom.example_output }}
  {% endif %}
  {% if product.metafields.custom.wk_custom_field["Example Output"] != "" %}
    {{ product.metafields.wk_custom_field["Example Output"] }}
  {% endif %}
</div>


{% if product.metafields.custom.seller_email != null and  product.metafields.custom.seller_email != "" %}
  <div>
    <h4>Contact info:</h4>
    {{ product.metafields.custom.seller_email }}
  </div>
{% else %}
{% render 'wk-contact-us' %}
{% endif %}

<div class="wk_seller_info clearfix" data-productid="{{ product.id }}" style="margin:10px 0;"></div>